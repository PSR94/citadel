# AWS Reference

Suggested mapping:

- ECS or EKS for `web` and `api`
- OpenSearch Service for lexical retrieval
- Aurora Postgres with pgvector or managed Postgres for metadata
- ElastiCache Redis for cache and coordination
- Neo4j Aura or self-managed graph
- CloudWatch and X-Ray for logs and tracing

