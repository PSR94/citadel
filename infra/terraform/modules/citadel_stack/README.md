# Terraform Reference Module

This directory is a reference seam, not a claimed multi-cloud deployment package.

Expected responsibilities:

- network and secret injection
- managed Postgres or equivalent relational store
- search, vector, graph, and cache services
- web and API service deployment
- observability sinks and identity provider wiring

